pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Build Test'
            }
        }
        
         stage('Test') {
            steps {
                echo 'Test Build'
            }
        }
        
         stage('Deploy') {
            steps {
                echo 'Deploy Build'
            }
        }
    }
}
